# CLAUDE.md

Read and follow `AGENTS.md` as the repository-wide source of truth.

When Claude Code is used, apply the same Spec-Driven Development lifecycle and the same `make setup`, `make check`, `make test`, and `make ci` commands. Do not duplicate repository policy in this file. Add only Claude-specific instructions here when a real behavioral difference is required.
